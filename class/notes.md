1. classes are just syntactic sugar for constructor fucntions and they still do protoypal inheritance

2) classes are not hoisted
   and classes are first class citizens , classes are executed in strict mode
